#include <stdio.h>
int main(){
    int a,b,c;
    scanf("%d %d %d", &a,&b,&c);
    switch(c){
        case +:
        result = a +b;
        printf("%d", result);
        break;
        case -:
        result = a - b;
        printf("%d", result);
        break;
        case *:
        result = a *b;
        printf("%d", result);
        break;
        case /:
        result = a/b;
        printf("%d", result);
        break;
    }
}